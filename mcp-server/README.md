# genai-lab MCP server

Spring Boot sub-project placeholder for Module 6.

The server will expose domain tools through Spring AI MCP server support and
will be consumed by the Python agent layer as an external MCP tool provider.

